// import { resolveConfig } from 'tailwindcss';
// import tailwindConfig from '../tailwind.config.js';

// export const Theme = resolveConfig(tailwindConfig).theme;

export const Theme = { colors: { accent: '#711c1c' } };
